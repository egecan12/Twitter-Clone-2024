extends layouts/login-layout.pug

block content 
    .loginContainer
        h1 Login 
        form(action="", method="post")
            input#usernameTextBox(type="text", name="logUsername", placeholder="Username or email", required="")
            input#usernameTextBox(type="password", name="logPassword", placeholder="Password", required="")
            input#usernameTextBox(type="submit", value="Login")
        a(href="/register") Need an account? Register here.